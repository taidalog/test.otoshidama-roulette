{
  "name": "otoshidama-roulette",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "dotnet fable watch src --run npx vite",
    "build": "dotnet fable src --run npx vite build",
    "build:testing": "dotnet fable src --run npx vite build --mode testing"
  },
  "keywords": [],
  "author": "taidalog",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "vite": "^5.4.11"
  }
}